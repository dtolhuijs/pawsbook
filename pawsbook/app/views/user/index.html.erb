<h1>Users</h1>

<% @users.each do |user| %>
    <!-- <img src = "<%= user.picture%>"> -->
    <% user.pictures.each do |pic| %>
      <%= pic.id %>
      <%= image_tag pic.name.url %>
    <% end %>
    <h2><%= user.name %></h2>
    <p> <%= user.gender%></p>
<% end %>
